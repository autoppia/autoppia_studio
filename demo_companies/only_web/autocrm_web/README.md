# AutoCRM Web

Web-only ICA wrapper for `autocrm` from `autoppia_webs_demo`.

This project intentionally contains only the ICA manifest. The runnable demo web lives in the legacy demo-web corpus and is exposed at `http://84.247.180.192:8004`.
