# Autoppia Studio Agent Notes

This file is for Codex sessions working in this repo:
`/home/usuario1/daryxx/autoppia/autoppia_studio`.

There is also a parent `../AGENTS.md` with broader Autoppia guidance. Prefer this
local file when instructions conflict because it describes the current Autoppia
Studio architecture and recent refactors.

As of 2026-06-19, no parent `../AGENTS.md` was present from this working
directory. If a parent file is recreated later, still prefer this local file for
Autoppia Studio-specific architecture, runtime, and production notes.

## Product Context

Autoppia Studio is the studio/backend for configuring agents, connectors, tools,
benchmarks, task harvesting, trajectory judging, and skill creation.

The runtime naming preferred by the user is:

- `AgentConfig`: the definition/configuration of an agent.
- `AgentRuntime`: the deployed runtime that executes `/step` using an
  `AgentConfig`.
- Avoid introducing new `operator` naming. Existing legacy names may remain only
  for compatibility, but new code and UI text should use `agent`.

The runtime service itself may be external. This repo creates and passes the
agent configuration; the external runtime decides model execution unless this
repo explicitly implements it.

## UI Work

For frontend/UI changes, the user prefers using Claude Code because it tends to
produce better UI work. Launch it from this repo when UI implementation is
substantial:

```bash
claude --dangerously-skip-permissions
```

If the installed command is exposed as `claude code`, use:

```bash
claude code --dangerously-skip-permissions
```

Still review and test the resulting changes. Codex should remain responsible for
integration, backend correctness, tests, and making sure the final app works.

Recent frontend state to preserve:

- Main navigation groups Agents under the Studio section. Do not put Agents in a
  separate or catch-all "Other" section.
- Benchmarks and Runs are separate pages/sections, not tabs on one page:
  `/evals` is Benchmarks and `/eval-runs` is Runs.
- Section headers such as "Connectors / Integrations your agents can call" use
  extra breathing room near borders. Keep the taller header spacing pattern
  (`min-h-16`, wider horizontal padding, and vertical padding) unless replacing
  it consistently across the app.

## Core Vocabulary

Use these meanings consistently:

- `Task`: the prompt plus everything needed to evaluate one thing. This includes
  start URL/state, seed/fixtures, success criteria, evaluator config, and any
  task metadata. A task is not a solution.
- `Benchmark`: a group of tasks, usually with shared metadata and run config.
- `Trajectory`: a concrete sequence of actions that attempts to solve a task.
  This is a solution candidate generated by a harvester or runtime.
- `Skill`: an approved trajectory promoted into a callable capability that the
  agent can use later at runtime.

Expected flow:

```text
Benchmark -> Tasks -> Harvester receives Task -> Candidate Trajectory
-> Judge evaluates Task + Trajectory -> approved Trajectory -> Skill
-> AgentRuntime can call Skill
```

Do not model pending work as rows in `trajectories`. Pending work belongs in
benchmark/task records. `trajectories` should represent generated solutions or
action traces.

## Data Model

Important Mongo collections in `backend/app/database.py`:

- `benchmarks_collection`: benchmark metadata.
- `benchmark_tasks_collection`: benchmark tasks.
- `trajectories_collection`: generated trajectories/action traces.
- skills/capabilities collections: approved callable skills.
- agents/connectors/tools/credentials collections: studio runtime config.

Legacy compatibility exists for old pending harvest rows in
`trajectories_collection` with `needs_harvest`, but new code should use
`benchmark_tasks_collection`.

Secrets and auth data should not be printed. If `.env` must be inspected, filter
or redact values.

## Local Development Stack

Current local deployment pattern used for this repo:

- Frontend: `http://127.0.0.1:3000`
- Backend API: `http://127.0.0.1:8080`
- MCP service: `http://127.0.0.1:5000`
- Mongo: Docker container `automata-mongo`
- PM2 apps commonly used locally:
  - `automata-cloud-backend`
  - `automata-cloud-worker`
  - `automata-cloud-frontend`
  - `automata-mcp`

Useful local commands:

```bash
pm2 status
pm2 logs automata-cloud-backend --lines 200
pm2 restart automata-cloud-backend automata-cloud-worker automata-cloud-frontend automata-mcp
docker ps
```

When changing backend code, restart the relevant PM2 backend/worker apps before
testing runtime behavior. When changing frontend code, run the package build or
lint command first, then restart `automata-cloud-frontend` if PM2 serves it.

## Harvesters

Main abstraction: `backend/app/services/agent_harvesters.py`.

Use:

- `AgentHarvester.harvest(agent_config)` for batch harvests.
- `AgentHarvester.harvest_task(agent_config, HarvestTask)` as the primitive.
- `harvest_one(...)` only exists as a legacy alias.

`HarvestTask` wraps a task record. Harvesters should receive tasks, not
trajectories.

Claude harvester timeout default is intentionally higher:

- File: `backend/app/harvester/claude_cli.py`
- Env var: `AUTOMATA_HARVESTER_TIMEOUT_SECONDS`
- Default: `600`

Full harvester benchmarks can be slow and can spend LLM/browser resources. Do
not rerun the full 14-web benchmark unless the user asks for it. Prefer small
smokes such as one project and one task.

## Judges

Main abstraction: `backend/app/services/trajectory_judges.py`.

`TrajectoryJudgeContext` is the shared input for all judge implementations. It
should carry everything a stateful evaluator or LLM judge needs:

- agent config
- normalized task spec
- trajectory/actions
- harvester result
- metadata
- artifacts
- evaluator config

Available judge implementations include:

- `rules`
- `hardcoded_html`
- `llm`
- `iwa`

The IWA judge should use the real IWA `AsyncStatefulEvaluator`; it is not just a
metadata/confidence placeholder. It should replay actions and score the final
state using IWA task definitions/golden tests when available.

The LLM judge should receive the same complete `TrajectoryJudgeContext`
serialized for judging, including artifacts when present.

## IWA And Demo Webs

Useful local paths:

- IWA repo: `/home/usuario1/daryxx/autoppia/operator/autoppia_iwa`
- Demo webs repo: `/home/usuario1/daryxx/autoppia/operator/autoppia_webs_demo`

Remote demo webs discovered from env/config are served from:

```text
http://84.247.180.192
```

with project ports starting around `8000`.

Canonical IWA projects found previously:

- `autocinema`
- `autobooks`
- `autozone`
- `autodining`
- `autocrm`
- `automail`
- `autodelivery`
- `autolodge`
- `autoconnect`
- `autowork`
- `autocalendar`
- `autolist`
- `autodrive`
- `autohealth`
- `autostats`

Known caveats from previous checks:

- Ports `8000..8013` were reachable in short checks.
- Ports around `8014`/`8015` timed out in short checks.
- `autostats` had no golden trajectories in the checked registry.
- `autodiscord` may exist in demo webs but was not part of the canonical
  evaluable IWA registry observed.

Recent successful smoke after the benchmark/task refactor:

- Project: `autocalendar`
- Task: `ADD_EVENT`
- IWA stateful evaluator replayed 6 actions.
- Score: `1/1`, raw `1.000`
- Result: one approved trajectory and one generated skill.

## Benchmark Script

Main script:

```bash
python scripts/iwa_harvester_benchmark.py --inventory-only
```

Small smoke example:

```bash
python scripts/iwa_harvester_benchmark.py \
  --project autocalendar \
  --tasks-per-project 1 \
  --harvester claude \
  --judge iwa \
  --verify-step
```

The script now creates/updates `benchmarks_collection` and
`benchmark_tasks_collection`. It should only create `trajectories` after harvest.

Useful options include:

- `--harvester`
- `--judge`
- `--harvest-concurrency`
- `--verify-step`

## API And Runtime

Prefer `/step` naming over `/act`. If legacy `/act` remains, it should be
compatibility only.

When reviewing runtime behavior, inspect exactly what `/step` receives. The
important question is whether `AgentConfig` is included and whether credentials,
tools, skills, connectors, and knowledge are available in the shape the runtime
expects.

Skills should be visible in the UI/chat timeline when used, so users can
distinguish "agent manually operated browser" from "agent called an approved
skill".

Connector runtime benchmark runbook:

```text
docs/connector_runtime_benchmarks.md
```

Use it when changing connector tools, runtime routing, skill replay, browser
panels, artifacts, approvals, or `/evals` connector benchmark UI. It documents
the live-without-skill and with-approved-skill audit matrix and the current
Celeris connector coverage.

## Email Agent

There is a local email-oriented runtime path for email operations:

- Runtime type: `local_email_agent`
- Runtime endpoint shape:
  `/runtime/agents/{agent_id}/step`
- Current seeded/local agent:
  - Name: `Email Operations Agent`
  - Agent ID: `54378693-19bd-4608-8a12-6727c633dbe1`
  - Owner: `demo@autoppia.com`
  - Company: `Celeris`
  - Company ID: `deae345c-8e98-42ec-a517-267b47f1488a`

Email tools currently expected for common email use cases:

- `imap.search_emails`: search inbox/messages.
- `imap.read_email`: read a specific message.
- `smtp.draft_email`: create a draft response.
- `smtp.send_email`: send mail. This requires human approval.
- Gmail schemas may also exist (`gmail.search_emails`, `gmail.read_email`,
  `gmail.send_email`), but they require connector auth before they are usable.

Useful runtime prompts for smoke testing the email agent:

- "Busca el email mas reciente sobre nominas, resume la peticion del cliente,
  clasificala y propon una respuesta sin enviarla."
- "Redacta un email profesional para cliente@example.com agradeciendo su
  consulta y diciendo que revisaremos el caso hoy. No lo envies; prepara asunto
  y cuerpo."
- "Envia un email para cliente@example.com con asunto Seguimiento y cuerpo
  Confirmamos que revisaremos su consulta hoy."

For email changes, verify both draft-only and send paths. The send path should
create an approval request instead of silently sending without review.

## Production: studio.autoppia.com

Production details verified on 2026-06-19:

- Public Studio SPA: `https://studio.autoppia.com`
- Public API host: `https://api-studio.autoppia.com`
- DNS/IP: `studio.autoppia.com` resolves to `161.97.163.78`
- Server hostname observed over SSH: `vmi2911219`
- App path on prod: `/opt/autoppia_studio`
- Nginx config path on prod host: `/opt/autoppia-web/nginx.conf`
- Nginx runs in Docker container `autoppia-web` from `nginx:alpine`.
- Mongo runs in Docker container `autoppia-studio-mongo` and is bound to
  `127.0.0.1:27017`.

Local SSH config to use:

```sshconfig
Host contabol_autoppia_landing
  HostName 161.97.163.78
  User root
  IdentityFile ~/.ssh/id_ed25519
  IdentitiesOnly yes
```

Connect with:

```bash
ssh contabol_autoppia_landing
cd /opt/autoppia_studio
```

Production process layout:

- `autoppia-studio-backend`: PM2-managed FastAPI/uvicorn backend.
  - CWD: `/opt/autoppia_studio/backend`
  - Command shape: source `.env`, set `AUTOMATA_ENV=production`, then run
    `.venv/bin/uvicorn main:app --host 127.0.0.1 --port 8100 --proxy-headers`
- `autoppia-studio-worker`: PM2-managed worker.
  - CWD: `/opt/autoppia_studio/backend`
  - Command shape: source `.env`, set `AUTOMATA_ENV=production`, then run
    `.venv/bin/python -m app.worker`
- Frontend is not a PM2 app in `ecosystem.studio.config.js`. It is served as
  static SPA files by the `autoppia-web` nginx container from
  `/srv/autoppia-studio`.

Nginx routing:

- `https://studio.autoppia.com` serves the static SPA and falls back to
  `index.html`.
- `https://api-studio.autoppia.com` proxies to `http://127.0.0.1:8100`.
- `/socket.io/` on `api-studio.autoppia.com` also proxies to
  `http://127.0.0.1:8100` with websocket headers and long timeouts.
- `https://studio.autoppia.com/health` currently returns the SPA HTML, not the
  backend health endpoint. Use the API host for backend checks.

Useful prod commands:

```bash
ssh contabol_autoppia_landing
cd /opt/autoppia_studio
pm2 status
pm2 logs autoppia-studio-backend --lines 200
pm2 logs autoppia-studio-worker --lines 200
pm2 restart autoppia-studio-backend autoppia-studio-worker
docker ps
curl -I https://studio.autoppia.com
curl -I https://api-studio.autoppia.com/health
```

Do not paste or commit production secrets. Prod backend environment lives in
`/opt/autoppia_studio/backend/.env`; inspect only key names or redacted values.
Known configured categories include Mongo, OpenAI, Google/Gmail OAuth, SMTP, and
knowledge/vector-store settings.

## Verification

Known recent baseline:

```bash
pytest backend/tests
```

passed with 57 tests.

Run this after backend/modeling/refactor changes. For frontend changes, also run
the relevant package lint/test/build command after inspecting `frontend` package
scripts.

Browser/IWA judge logs can be noisy and may print event streams. Focus on final
judge label, score, trajectory status, skill creation, and whether actions
replayed correctly.

## Git And Editing Safety

The worktree may contain many existing user or generated changes. Do not reset,
checkout, or revert files unless explicitly requested.

Use `apply_patch` for manual edits. Keep changes scoped. Avoid broad formatting
or unrelated refactors.

When in doubt, preserve compatibility for legacy data and routes while making
new code use the clearer task/benchmark/trajectory/skill model.
